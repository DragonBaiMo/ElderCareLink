spring:
  datasource:
    url: jdbc:mysql://localhost:3306/eldercare?useUnicode=true&characterEncoding=utf8mb4&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  jackson:
    time-zone: Asia/Shanghai
    date-format: yyyy-MM-dd'T'HH:mm:ss
server:
  port: 8080

security:
  jwt:
    # 使用 openssl rand -base64 48 生成的演示秘钥
    secret: iXQNSCln2YtuH5XRrqCXM15vLk7mKrtZae2yWkf0h0GoHh4pkeH9qQ==
    ttlMinutes: 240

logging:
  level:
    com.eldercarelink: INFO
